spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/spring_boot_ecommerce
    password: root
  jpa:
    hibernate:
      ddl-auto: update
  application:
    name: Spring-Boot-ECommerce
  data:
    rest:
      base-path: /api
